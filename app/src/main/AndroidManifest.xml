<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.project.delcanteen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DelCanteen"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.BaruActivity"
            android:exported="false" />
        <activity
            android:name="com.project.delcanteen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false" />
        <activity android:name=".activity.MasukActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.ProdukKantinActivity" />
        <activity android:name=".activity.ProdukKoperasiActivity" />
        <activity android:name=".activity.PeminjamanRuanganActivity" />
        <activity android:name=".activity.PulsaActivity" />
    </application>

</manifest>